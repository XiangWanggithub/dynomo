# 📋 保留文件清单

## ✅ CustomDataset核心文件

### 数据集实现
```
src/datasets/datasets/
├── __init__.py              ← 已简化，只导入CustomDataset
├── basedataset.py          ← CustomDataset的父类
├── datautils.py            ← 数据处理工具
├── geometryutils.py        ← 几何变换工具
├── col_map_utils.py        ← 颜色映射工具
└── custom_dataset.py       ← ⭐ CustomDataset实现
```

### 训练和推理脚本
```
scripts/
├── train_custom.py         ← ⭐ 训练/微调入口（入口1）
└── inference_custom.py     ← ⭐ 推理入口（入口2）
```

### 配置文件
```
configs/
├── custom/
│   └── dynomo_custom.py    ← 主配置文件
└── data/
    └── custom.yaml         ← 相机参数配置
```

### 工具脚本
```
examples/
└── prepare_custom_data.py  ← 数据验证工具

根目录/
└── test_custom_setup.py    ← 安装验证脚本
```

## ✅ 通用预处理工具（CustomDataset需要）

```
preprocess/
├── __init__.py
├── get_depth_anything_prediction.py     ← 深度估计
├── get_depth_anything_V2_prediction.py  ← 深度估计V2
└── get_dino_prediction.py               ← DINO特征提取
```

## ✅ 核心模型代码（完整保留）

```
src/
├── model/                   ← DynOMo核心实现
│   ├── __init__.py
│   ├── dynomo.py           ← 主模型类
│   ├── logger.py
│   ├── optimization.py
│   ├── renderer.py
│   └── scene.py
│
├── evaluate/               ← 评估工具
│   ├── __init__.py
│   ├── rendering_evaluator.py
│   └── trajectory_evaluator.py
│
└── utils/                  ← 工具函数
    ├── __init__.py
    ├── camera_helpers.py
    ├── common_utils.py
    ├── gaussian_utils.py
    ├── get_data.py         ← 已简化
    ├── losses.py
    ├── neighbor_search.py
    └── viz_utils.py
```

## ✅ 文档文件

```
根目录/
├── README.md                      ← 主文档（已更新为CustomDataset）
├── 清理完成-请阅读.txt            ← 简要总结
├── 重构完成说明.md                ← 重构完成说明
├── QUICKSTART_CUSTOM.md           ← 快速开始指南
├── CUSTOM_DATASET_README.md       ← 完整使用文档
├── REFACTOR_SUMMARY.md            ← 详细重构总结
├── PROJECT_STRUCTURE.md           ← 项目结构说明
├── FILES_CHECKLIST.md             ← 新增文件清单
├── CLEANUP_SUMMARY.md             ← 详细清理总结
├── 最终清理总结.md                ← 完整清理报告
└── 保留文件清单.md                ← 本文档
```

## ✅ 其他重要文件

```
根目录/
├── .gitignore
├── LICENSE
├── environment.yml
├── requirements.txt
└── scripts/
    ├── create_env.sh
    ├── prepare_data.py
    └── export_ply.py
```

## 📊 文件统计

| 类别 | 文件数 |
|------|--------|
| CustomDataset核心 | 11个 |
| 预处理工具 | 4个 |
| 核心模型 | ~30个 |
| 文档 | 11个 |
| 其他 | ~10个 |
| **总计** | **~66个** |

## 🎯 关键文件说明

### 必须使用的文件

1. **`custom_dataset.py`** - CustomDataset数据集类
2. **`train_custom.py`** - 训练入口
3. **`inference_custom.py`** - 推理入口
4. **`dynomo_custom.py`** - 配置文件
5. **`custom.yaml`** - 相机参数

### 支持文件（必需）

6. **`basedataset.py`** - CustomDataset的父类
7. **`datautils.py`** - 数据处理
8. **`get_data.py`** - 数据加载
9. **深度和特征提取脚本** - 在线处理需要

### 核心模型（必需）

10. **`src/model/`** - 完整的DynOMo实现
11. **`src/evaluate/`** - 评估工具
12. **`src/utils/`** - 工具函数

## 📝 使用说明

所有保留的文件都是CustomDataset正常工作所必需的：

- **不要删除** `basedataset.py` - CustomDataset继承自它
- **不要删除** `datautils.py` - 数据处理需要
- **不要删除** `geometryutils.py` - 几何变换需要
- **不要删除** 预处理脚本 - 在线深度和特征提取需要
- **不要删除** `src/model/` - 核心模型实现

## ✨ 代码组织

```
核心入口 (2个)
    ├── train_custom.py        → 训练
    └── inference_custom.py    → 推理
         ↓
配置文件 (2个)
    ├── dynomo_custom.py       → 主配置
    └── custom.yaml           → 相机参数
         ↓
数据加载 (1个)
    └── custom_dataset.py      → 数据集
         ↓
基础支持 (3个)
    ├── basedataset.py
    ├── datautils.py
    └── geometryutils.py
         ↓
核心模型
    ├── src/model/
    ├── src/evaluate/
    └── src/utils/
```

---

**所有保留的文件都经过验证，确保CustomDataset正常工作！**
